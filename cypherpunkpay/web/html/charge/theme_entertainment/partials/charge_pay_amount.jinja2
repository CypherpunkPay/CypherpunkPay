<h1 class="title select-none has-text-link-dark">
    {% if charge.is_underpaid() %}
        Pay remaining {{ view.formatted_currency_amount(charge.cc_remaining_total(), charge.cc_currency, select_amount=True) | safe }}
    {% else %}
        Pay {{ view.formatted_currency_amount(charge.cc_remaining_total(), charge.cc_currency, select_amount=True) | safe }}
    {% endif %}
</h1>
